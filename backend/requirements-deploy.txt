# Raidex Backend — MongoDB deployment (Render free tier)
# Canonical, tested dependency set: requirements.txt. This file is a deliberate
# deploy-trimmed SUBSET of it (no dev/test/lint tooling: pytest, black, flake8,
# mypy, pandas, jq, typer, etc) to keep the free-tier build fast and small -
# it is not meant to be collapsed into one file. Whenever you bump a version in
# requirements.txt, mirror the same version here if the package appears in both.
fastapi==0.138.0
uvicorn==0.25.0
python-dotenv>=1.0.1
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
passlib>=1.7.4
slowapi>=0.1.9
httpx>=0.27.0
python-multipart>=0.0.9
apscheduler>=3.10.4
sentry-sdk>=2.13.0
anthropic>=0.39.0
